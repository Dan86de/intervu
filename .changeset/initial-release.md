---
"intervu": minor
---

First published release: intervu ships as an installable npm package - run `bunx intervu <file>` (or `bun add -g intervu`) on any machine with Bun - with releases automated via Changesets.
